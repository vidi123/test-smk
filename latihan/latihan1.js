/**
 * Menjumlahkan a dan b
 * @namespace Latihan
 * @method sum
 * @param {Number} a number
 * @param {Number} b number
 * @return {Number} hasil number
 */
function sum(a, b) {
  // TODO: jumlahkan a dan b kemudian return hasilnya
  return parseFloat((a + b).toFixed(1));
}
module.exports = sum;
